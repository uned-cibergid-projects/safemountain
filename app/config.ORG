const VERSION = '1.0.1';
const APP = 'ciber';
const CARPETAS = {
    // Aquí se definen las carpetas que utiliza el aplicativo NodeJs; si no existen se crean
    carpeta1: "./public",
    carpeta2: "./public/logs",
    carpeta3: "./public/procesos"
};
const DEBUG = 'http:*,mail:*,express:*,ciber:*';

module.exports = {
   'desarrollo': {
        ENTORNO: 'desarrollo',
        VERSION: VERSION,
        APP: APP,
        PORT: 8020,
        MONGO: {
            db: 'pruebas',
            host: '10.201.54.162',
            user: '',
            pass: '',
            port: 49016
        },
        AVIP: AVIP,
        CARPETAS: CARPETAS,
        DEBUG: DEBUG
    },
}